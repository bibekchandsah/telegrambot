# Ban/Unban System Architecture

## System Flow Diagram

```
┌─────────────────────────────────────────────────────────────────────┐
│                        BAN/UNBAN SYSTEM                             │
└─────────────────────────────────────────────────────────────────────┘

┌──────────────┐        ┌──────────────┐        ┌──────────────┐
│    ADMIN     │        │     USER     │        │    SYSTEM    │
│  Commands    │        │  Protection  │        │  Auto-Ban    │
└──────┬───────┘        └──────┬───────┘        └──────┬───────┘
       │                       │                       │
       │                       │                       │
       ├─► /ban ───────────────┼─────────────┐        │
       │   • User ID           │             │        │
       │   • Reason            │             ▼        │
       │   • Duration      ┌───────────────────┐     │
       │                   │   REDIS STORAGE   │     │
       ├─► /unban ─────────┤   ┌─────────────┐ │     │
       │                   │   │ ban:{id}    │ │     │
       ├─► /warn ──────────┤   │ warnings:{} │ │     │
       │                   │   │ ban_history │ │     │
       ├─► /checkban ──────┤   │ warning_list│ │     │
       │                   │   └─────────────┘ │     │
       ├─► /bannedlist     └───────────────────┘     │
       │                           │                  │
       └─► /warninglist            │                  │
                                   │                  │
                                   ▼                  │
                           ┌───────────────┐         │
                           │  CHECK BAN    │◄────────┤
                           │   STATUS      │         │
                           └───────┬───────┘         │
                                   │                  │
                    ┌──────────────┼─────────────┐   │
                    ▼              ▼             ▼   │
            ┌──────────┐   ┌──────────┐  ┌──────────┴─────┐
            │ Message  │   │  Chat    │  │ Report Counter │
            │ Handler  │   │ Command  │  │   (5 reports)  │
            └──────────┘   └──────────┘  └────────────────┘
                                                    │
                                                    ▼
                                          ┌─────────────────┐
                                          │   AUTO-BAN      │
                                          │   7 days temp   │
                                          └─────────────────┘
```

## Command Flow Diagrams

### 1. Ban User Flow
```
/ban command
    │
    ▼
Enter User ID
    │
    ▼
Select Reason ───┬─► Nudity/Explicit
                 ├─► Spam
                 ├─► Abuse
                 ├─► Fake Reports
                 └─► Harassment
    │
    ▼
Select Duration ─┬─► 1 Hour
                 ├─► 24 Hours
                 ├─► 7 Days
                 ├─► 30 Days
                 └─► Permanent
    │
    ▼
Execute Ban ─────┬─► Save to Redis
                 ├─► Update ban list
                 ├─► Notify user
                 └─► Confirm to admin
    │
    ▼
User Blocked from:
    • Sending messages
    • Joining queue
    • Using bot features
```

### 2. Auto-Ban Flow
```
User reports another user (/report)
    │
    ▼
Increment report counter (Redis)
    │
    ▼
Check threshold (5 reports)
    │
    ├─► Less than 5: Continue
    │
    └─► 5 or more:
        │
        ▼
    Auto-Ban Triggered
        │
        ├─► Duration: 7 days
        ├─► Reason: "abuse"
        ├─► Logged as system ban
        └─► User notified
```

### 3. Message Check Flow
```
User sends message
    │
    ▼
Check Redis: ban:{user_id}
    │
    ├─► Not Banned ─────► Forward message to partner
    │
    └─► Banned ─────────┬─► Block message
                        ├─► Send ban notification
                        └─► Log attempt
```

## Data Structure Diagram

```
┌─────────────────────────────────────────────────────────────┐
│                     REDIS STORAGE                           │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  BAN DATA                                                   │
│  ┌────────────────────────────────────────────┐            │
│  │ ban:{user_id}                              │            │
│  │ {                                          │            │
│  │   "user_id": 123456789,                   │            │
│  │   "banned_by": 987654321,                 │            │
│  │   "reason": "spam",                       │            │
│  │   "banned_at": 1701234567,                │            │
│  │   "expires_at": 1701838367,               │            │
│  │   "is_permanent": false,                  │            │
│  │   "is_auto_ban": false                    │            │
│  │ }                                          │            │
│  └────────────────────────────────────────────┘            │
│                                                             │
│  TRACKING SETS                                              │
│  ┌────────────────────────────────────────────┐            │
│  │ bot:banned_users      → {id1, id2, ...}   │            │
│  │ bot:warning_list      → {id3, id4, ...}   │            │
│  └────────────────────────────────────────────┘            │
│                                                             │
│  COUNTERS                                                   │
│  ┌────────────────────────────────────────────┐            │
│  │ warning_count:{user_id}  → 3              │            │
│  │ stats:{user_id}:report_count → 5          │            │
│  └────────────────────────────────────────────┘            │
│                                                             │
│  HISTORY (LISTS)                                            │
│  ┌────────────────────────────────────────────┐            │
│  │ ban_history:{user_id}    → [ban1, ban2]   │            │
│  │ unban_history:{user_id}  → [unban1, ...]  │            │
│  │ warnings:{user_id}       → [warn1, ...]   │            │
│  └────────────────────────────────────────────┘            │
└─────────────────────────────────────────────────────────────┘
```

## Component Interaction Diagram

```
┌─────────────────────────────────────────────────────────────────────┐
│                    TELEGRAM BOT COMPONENTS                          │
└─────────────────────────────────────────────────────────────────────┘

┌─────────────────┐
│   bot.py        │
│  (Main App)     │
└────────┬────────┘
         │
         ├──────────┬──────────┬──────────┬──────────┐
         │          │          │          │          │
         ▼          ▼          ▼          ▼          ▼
┌─────────────┐ ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌─────────┐
│  commands.py│ │messages │ │ admin.py│ │matching │ │feedback │
│             │ │  .py    │ │ (Ban)   │ │  .py    │ │  .py    │
│ • /ban      │ │         │ │         │ │         │ │         │
│ • /unban    │ │ handle_ │ │ ban_    │ │ find_   │ │ record_ │
│ • /warn     │ │ message │ │ user()  │ │ partner │ │ report  │
│ • /checkban │ │    ▼    │ │   ▼     │ │         │ │    ▼    │
│ • /banned   │ │  Check  │ │  Check  │ │         │ │  Check  │
│   list      │ │   Ban   │ │   Ban   │ │         │ │  Auto-  │
│ • /warning  │ │         │ │         │ │         │ │   Ban   │
│   list      │ │         │ │         │ │         │ │         │
└─────┬───────┘ └────┬────┘ └────┬────┘ └─────────┘ └─────────┘
      │              │           │
      │              │           │
      └──────────────┴───────────┴──────────────┐
                                                 │
                                                 ▼
                                    ┌─────────────────────┐
                                    │  redis_client.py    │
                                    │                     │
                                    │  • get()           │
                                    │  • set()           │
                                    │  • sadd()          │
                                    │  • smembers()      │
                                    │  • incr()          │
                                    │  • lpush()         │
                                    │  • expire()        │
                                    └─────────────────────┘
```

## User Journey Diagram

### Scenario: User Gets Banned

```
┌─────────┐
│  USER   │ Violates Rules (e.g., sends spam)
└────┬────┘
     │
     ▼
┌─────────┐
│ PARTNER │ Reports via /report
└────┬────┘
     │
     ▼
┌──────────────────┐
│ REPORT SYSTEM    │ Increments counter
│ Counter: 1 → 5   │
└────┬─────────────┘
     │
     ▼
┌──────────────────┐
│ AUTO-BAN CHECK   │ Threshold reached!
│ 5 reports = Ban  │
└────┬─────────────┘
     │
     ▼
┌──────────────────┐
│ BAN EXECUTED     │ 7-day temporary ban
│ Reason: "abuse"  │
└────┬─────────────┘
     │
     ├─► User notified: "🚫 You are temporarily banned"
     │
     └─► Ban recorded in Redis with expiry
     
     
User tries to send message:
     │
     ▼
┌──────────────────┐
│ MESSAGE HANDLER  │ Checks ban status
│ is_user_banned() │
└────┬─────────────┘
     │
     ├─► Banned? ✅
     │
     └─► Block message + Show notification
     

7 Days Later (Auto-expiry):
     │
     ▼
┌──────────────────┐
│ REDIS TTL EXPIRES│ Ban removed automatically
└────┬─────────────┘
     │
     ▼
┌──────────────────┐
│ USER UNBANNED    │ Can use bot again
└──────────────────┘
```

### Scenario: Admin Manual Ban

```
┌──────────┐
│  ADMIN   │ Types /ban
└────┬─────┘
     │
     ▼
┌──────────────────┐
│ ENTER USER ID    │ 123456789
└────┬─────────────┘
     │
     ▼
┌──────────────────┐
│ SELECT REASON    │ Clicks "Harassment"
└────┬─────────────┘
     │
     ▼
┌──────────────────┐
│ SELECT DURATION  │ Clicks "30 Days"
└────┬─────────────┘
     │
     ▼
┌──────────────────┐
│ EXECUTE BAN      │
│ • Save to Redis  │
│ • Set expiry     │
│ • Log action     │
└────┬─────────────┘
     │
     ├─► Admin: "✅ User Banned Successfully"
     │
     └─► User: "🚫 You are temporarily banned"


Admin checks later:
     │
     ▼
┌──────────────────┐
│ /checkban 123456│ Shows full ban details
└──────────────────┘
```

## Integration Points

```
┌─────────────────────────────────────────────────────────────┐
│                  BAN SYSTEM INTEGRATIONS                    │
└─────────────────────────────────────────────────────────────┘

┌───────────────┐         ┌───────────────┐
│ CHAT COMMAND  │────────▶│  BAN CHECK    │
│   /chat       │         │  Before Queue │
└───────────────┘         └───────────────┘

┌───────────────┐         ┌───────────────┐
│ MESSAGE       │────────▶│  BAN CHECK    │
│ HANDLER       │         │  Before Send  │
└───────────────┘         └───────────────┘

┌───────────────┐         ┌───────────────┐
│ REPORT        │────────▶│  AUTO-BAN     │
│ SYSTEM        │         │  CHECK        │
└───────────────┘         └───────────────┘

┌───────────────┐         ┌───────────────┐
│ PROFILE       │────────▶│  BAN STATUS   │
│ SYSTEM        │         │  PRESERVED    │
└───────────────┘         └───────────────┘

┌───────────────┐         ┌───────────────┐
│ RATING        │────────▶│  BAN STATUS   │
│ SYSTEM        │         │  PRESERVED    │
└───────────────┘         └───────────────┘
```

## Performance Flow

```
User Message → Ban Check (O(1)) → Forward or Block
                    │
                    └─► Redis GET operation
                            │
                            └─► ~1ms response time
                            
Report → Increment → Check Threshold → Auto-Ban
    ↓        ↓            ↓              ↓
  O(1)     O(1)         O(1)          O(1)
  ~1ms     ~1ms         ~1ms          ~5ms


Temporary Ban → Redis TTL → Auto-Expire
                    │
                    └─► No manual cleanup needed
```

## Security Layer

```
┌─────────────────────────────────────────────────────────────┐
│                    SECURITY CHECKS                          │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  Layer 1: Admin Permission Check                            │
│  ┌────────────────────────────────────┐                    │
│  │ is_admin(user_id)                  │                    │
│  │ • Checks against ADMIN_IDS         │                    │
│  │ • Returns 403 if not admin         │                    │
│  └────────────────────────────────────┘                    │
│                                                             │
│  Layer 2: Ban Status Check                                  │
│  ┌────────────────────────────────────┐                    │
│  │ is_user_banned(user_id)            │                    │
│  │ • Checks Redis ban:{user_id}       │                    │
│  │ • Validates expiry                 │                    │
│  │ • Returns ban details              │                    │
│  └────────────────────────────────────┘                    │
│                                                             │
│  Layer 3: Audit Trail                                       │
│  ┌────────────────────────────────────┐                    │
│  │ Log all actions with:              │                    │
│  │ • Admin ID                         │                    │
│  │ • Target user ID                   │                    │
│  │ • Action type                      │                    │
│  │ • Timestamp                        │                    │
│  │ • Reason                           │                    │
│  └────────────────────────────────────┘                    │
│                                                             │
│  Layer 4: User Notification                                 │
│  ┌────────────────────────────────────┐                    │
│  │ Always notify user about:          │                    │
│  │ • Ban with reason & duration       │                    │
│  │ • Warning with reason              │                    │
│  │ • Unban confirmation               │                    │
│  └────────────────────────────────────┘                    │
└─────────────────────────────────────────────────────────────┘
```

---

This architecture ensures:
- ✅ Fast performance (O(1) operations)
- ✅ Automatic expiry (Redis TTL)
- ✅ Complete audit trail
- ✅ User transparency
- ✅ Admin control
- ✅ System automation
